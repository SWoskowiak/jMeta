{
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.7",
    "dirty-chai": "^2.0.1",
    "mocha": "^6.2.2",
    "nyc": "^14.1.1"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "scripts": {
    "test": "node_modules/.bin/mocha jmeta.test.js --exit --timeout 5000",
    "coverage": "nyc report",
    "report": "nyc report --reporter=text-lcov | coveralls",
    "coveralls": "yarn report"
  }
}
